import axion


def test_builtin_plugins_detected() -> None:
    assert len(axion._plugins()) == 1
    assert len(axion._plugins()) == 1
    assert len(axion._plugins()) == 1
    assert len(axion._plugins()) == 1
