---
- case: test_str
  disable_cache: true
  main: |
    test = "str"
    reveal_type(test)  # N: Revealed type is 'builtins.str'
- case: test_int
  disable_cache: true
  main: |
    test = int
    reveal_type(test)  # N: Revealed type is 'builtins.int'