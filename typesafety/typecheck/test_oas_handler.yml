---
- case: not_oas_handler
  disable_cache: true
  main: |
    from axion import oas_endpoint
    from axion import response

    @oas_endpoint
    async def not_oas_handler(  # E: myapp.not_oas_handler is not OAS operation  [axion-no-op]
    ) -> response.Response:
        return {
            'http_code': 200,
        }
  files:
    - path: myapp/init__.py
